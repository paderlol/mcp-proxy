[package]
name = "mcp-proxy-agent"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "mcp-proxy-agent"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
zeroize = { version = "1", features = ["derive"] }
